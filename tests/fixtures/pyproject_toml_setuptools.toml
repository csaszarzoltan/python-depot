[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "example-pkg"
version = "0.1.0"
description = "An example package"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.0.0",
]
