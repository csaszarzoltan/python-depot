[build-system]
requires = ["poetry-core>=1.5.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "example-poetry-pkg"
version = "0.2.0"
description = "A Poetry-managed package"
authors = ["Test User <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.0"
click = "^8.1.0"
