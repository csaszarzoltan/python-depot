#!/usr/bin/env python3
"""
Catalog API Examples

This directory contains working, runnable examples for the Catalog API endpoints.

## Prerequisites

These examples assume you have PythonDepot running locally:

```bash
# From the project root directory
cd python-depot

# Start the development server
uvicorn src.app:app --reload
```

## Examples

### Example 1: Basic Package Operations
File: `basic_operations.py`

This example demonstrates basic CRUD operations for packages.

Run with:
```bash
python basic_operations.py
```

### Example 2: Advanced Catalog Search and Filtering
File: `advanced_search.py`

This example demonstrates advanced catalog operations.

Run with:
```bash
python advanced_search.py
```

### Example 3: Error Handling and Validation
File: `error_handling.py`

This example demonstrates proper error handling and validation.

Run with:
```bash
python error_handling.py
```

### Example 4: Batch Operations and Performance Testing
File: `batch_ops.py`

This example demonstrates batch operations and performance testing.

Run with:
```bash
python batch_ops.py
```

## Usage

1. Start the PythonDepot server:
```bash
cd python-depot
uvicorn src.app:app --reload
```

2. Run any example:
```bash
cd examples
cat README.md
```

3. Each example will automatically test the Catalog API endpoints
   and clean up any created resources.

## Dependencies

These examples require:
- httpx (for async HTTP requests)
- The PythonDepot server running locally

Install dependencies:
```bash
cd python-depot
pip install -e ".[dev]"
```

## Testing Environment

Each example will:
- Test all major Catalog API endpoints
- Create and clean up test resources
- Handle errors gracefully
- Provide detailed output of operations
- Measure performance where applicable

## Error Handling

Each example includes comprehensive error handling:
- Network connectivity checks
- API response validation
- Resource cleanup on failure
- Detailed error reporting

## Customization

You can customize these examples by:
1. Changing the `base_url` to point to a different server
2. Modifying the package data in the examples
3. Adding your own test cases
4. Adjusting timing parameters

## Troubleshooting

### Server Not Running
```bash
# Start the server
cd python-depot
uvicorn src.app:app --reload
```

### Port Conflicts
Change the `base_url` in examples to use a different port:
```python
base_url = "http://localhost:8001"
```

### Missing Dependencies
```bash
pip install httpx
```

This README was generated as part of the documentation task for PythonDepot.

---
*Generated by: Documenter Profile for PythonDepot*